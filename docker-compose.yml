version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: mysql-db-cechriza
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cechriza_web
      MYSQL_USER: werner
      MYSQL_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data: